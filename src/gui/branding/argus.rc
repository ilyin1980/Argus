// Windows resource script: the icon Explorer, the taskbar and Alt+Tab show.
// IDI_ICON1 is the conventional first icon; Windows picks the lowest-numbered
// one for the executable, so the name matters as much as the file does.
IDI_ICON1 ICON "argus.ico"
